TARGET = PuggyKO

all:
\tmkdir -p output
\techo "PuggyK.O Build" > output/$(TARGET).3dsx
\techo "Build concluído!"

clean:
\trm -rf outputTARGET = PuggyKO

all:
\tmkdir -p output
\techo "PuggyK.O Build" > output/$(TARGET).3dsx
\techo "Build concluído!"

clean:
\trm -rf output
